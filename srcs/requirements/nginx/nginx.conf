server {
  listen 443 ssl http2;  # Listen on port 443 with TLSv1.2/1.3 and HTTP/2 (replace with desired protocol)

  # Replace 'your_server_name' with your actual server name
  server_name my_server;

  # Replace 'path/to/your.crt' and 'path/to/your.key' with the actual paths to your SSL certificate and key
  # ssl_certificate path/to/your.crt;
  # ssl_certificate_key path/to/your.key;

  # Configure your website content location and other settings here
  location / {
    # ... (your website content configuration)
  }
}